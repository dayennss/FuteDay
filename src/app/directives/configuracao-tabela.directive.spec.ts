import { ConfiguracaoTabelaDirective } from './configuracao-tabela.directive';

describe('ConfiguracaoTabelaDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfiguracaoTabelaDirective();
    expect(directive).toBeTruthy();
  });
});
